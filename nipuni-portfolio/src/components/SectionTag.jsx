// SectionTag.jsx — Small animated pill label shown above section titles.
export default function SectionTag({ children }) {
  return (
    <span className="section-tag">
      {children}
    </span>
  )
}
